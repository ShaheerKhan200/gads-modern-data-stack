beamdata_dbt:
  outputs:
    qa:           #use dbt build --target qa
      dbname: weclouddwh
      host: 13.59.217.187
      pass: shaheer_password
      port: 5432
      schema: gads_qa # 
      threads: 1
      type: postgres
      user: shaheer_user
    dev:        #use dbt build --target dev or dbt build
      dbname: "{{ env_var('DBT_ENV_SECRET_DBNAME') }}"
      host: "{{ env_var('DBT_ENV_SECRET_HOST') }}"
      pass: "{{ env_var('DBT_ENV_SECRET_PASS') }}"
      port: 5432
      schema: gads_dev
      threads: 1
      type: postgres
      user: "{{ env_var('DBT_ENV_SECRET_USER') }}"
    prod:       #use dbt build --target prod
      dbname: weclouddwh
      host: 13.59.217.187
      pass: shaheer_password
      port: 5432
      schema: gads_prod # 
      threads: 1
      type: postgres
      user: shaheer_user
  target: dev